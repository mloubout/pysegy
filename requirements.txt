# Streamlit Community Cloud runs the app by its file path, so the repository
# root is not added to Python's import path. Install the local project as well
# as its viewer dependencies so ``import pysegy`` works during app startup.
-e .

# Keep these constraints aligned with the core and viewer dependency groups in
# pyproject.toml.
numpy>=2,<3
fsspec>=2024.3
cloudpickle>=3
matplotlib>=3.7
colorcet>=3
pandas>=2
plotly>=5
streamlit>=1.50
